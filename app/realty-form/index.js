const { validateRealtyForm } = require('./validator');
const { REALTY_FORM_FIELDS, REALTY_FORM_FIELDS_IDS } = require('./fields');

module.exports = {
    validateRealtyForm,
    REALTY_FORM_FIELDS,
    REALTY_FORM_FIELDS_IDS
};
